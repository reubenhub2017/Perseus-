class Template:
    def __init__(self, frame):
        pass
    def basicNodeTemplate(self):
        root = Tk()
        root.title(windowTitle)
        root.geometry('height '+ 'x' + 'width')
        LeftFrame = Frame(root, height = h,width = wh, relief=SUNKEN, bd=2, bg="tan")
        rightFrame = Frame(root, height=h, widht=wh, relief=SUNKEN, bd=2, bg="tan")
        TopButton = Button()
        #Need to see how to install color picker

    def basicEdgeTemplate(self):
        root = Tk()
        root.title(windowTitle)
        root.geometry('height '+ 'x' + 'width')
        LeftFrame = Frame(root, height = h,width = wh, relief=SUNKEN, bd=2, bg="tan")
        rightFrame = Frame(root, height=h, widht=wh, relief=SUNKEN, bd=2, bg="tan")

    def SignInTemplate(self):
        pass
    def SignOutTemplate(self):
        pass
